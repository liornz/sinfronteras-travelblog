import styles from './destination-map.module.scss';

interface Props {

}

const DestinationMap: React.FC<Props> = (props) => {
const {  } = props;
  return (
    <div className={styles.map}>
      <h3>The Map will come here</h3>
    </div>
  );
};

export default DestinationMap;